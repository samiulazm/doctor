; PHP per-directory settings (read by cPanel/LiteSpeed). Upload next to index.php.
; If the site still errors, temporarily rename this file to .user.ini.bak to test.

max_input_vars = 20000

; PHP 8.3+ only — uncomment on 8.3+ if you need it (do not use on older PHP):
; max_multipart_body_parts = 20000

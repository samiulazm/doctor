RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php?/$1 [L]

# php_value often breaks LiteSpeed shared hosting (HTTP 500). Use .user.ini in Multi-Hospital/.


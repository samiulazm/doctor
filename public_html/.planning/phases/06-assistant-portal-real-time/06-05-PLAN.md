---
phase: 06-assistant-portal-real-time
plan: "05"
type: execute
wave: 3
depends_on:
  - "06-04"
files_modified: []
autonomous: false
requirements:
  - UI-12
  - UI-13
  - PAY-01
  - QUEUE-01

must_haves:
  truths:
    - "Full PHPUnit suite is green after all Phase 6 plans execute"
    - "All 20+ AssistantPortal test methods pass"
    - "Phase 6 conformance checklist items are human-verified against a running dev instance"
    - "No regressions in prior phase test suites (Security, CodeHealth, DesignSystem, PatientPortal)"
  artifacts:
    - path: "tests/Unit/AssistantPortal/AssistantPortalTest.php"
      provides: "All tests green"
      contains: "function test_"
  key_links:
    - from: "composer test"
      to: "all test suites"
      via: "PHPUnit"
      pattern: "OK|PASS"
---

<objective>
Integration smoke test and human verification of the complete Phase 6 delivery.

Purpose: Confirm all automated tests pass and the four UI-SPEC conformance checklists are met against a running dev server.

Output: Full test suite green; human sign-off on drag-drop, emergency bump, bKash initiate, fee record, and real-time queue sync.
</objective>

<execution_context>
@C:\Users\Samiul\Pictures\doctor\public_html\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Samiul\Pictures\doctor\public_html\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Samiul\Pictures\doctor\public_html\.planning\PROJECT.md
@C:\Users\Samiul\Pictures\doctor\public_html\.planning\phases\06-assistant-portal-real-time\06-VALIDATION.md
@C:\Users\Samiul\Pictures\doctor\public_html\.planning\phases\06-assistant-portal-real-time\06-UI-SPEC.md
@C:\Users\Samiul\Pictures\doctor\public_html\.planning\phases\06-assistant-portal-real-time\06-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Full test suite smoke run</name>
  <files></files>
  <read_first>
    - .planning/phases/06-assistant-portal-real-time/06-04-SUMMARY.md — confirm all prior plans completed; if any plan failed, stop and report before running tests
  </read_first>
  <action>
Run the full PHPUnit test suite and report results:

1. Run: `composer test -- --filter AssistantPortal` — all 20+ AssistantPortal tests must pass
2. Run: `composer test` — full suite; zero failures permitted
3. Grep-verify the migration file exists: `grep -c "is_emergency" application/migrations/20260427000012_chamber_queue_is_emergency.php`
4. Grep-verify all 3 CSS classes: `grep -c "chamber-status.emergency\|chamber-source-tag\|btn-bkash" common/css/chamber-practice.css`
5. Grep-verify 4 new controller methods:
   - `grep -c "function queue_ticker_json\|function print_token\|function mark_queue_fee_paid_ajax" application/modules/assistant_chamber/controllers/Assistant_chamber.php`
   - `grep -c "function bkash_initiate_desk" application/modules/payment_bd/controllers/Payment_bd.php`
6. Grep-verify view files:
   - `grep -c "bkashSection_\|paidDueToggle_" application/modules/assistant_chamber/views/assistant/_billing_panel.php`
   - `grep -c "window.print" application/modules/assistant_chamber/views/assistant/token_print.php`

If any grep or test fails: DO NOT proceed to the checkpoint. Report the specific failure and which plan needs to be re-executed or patched.

If all 6 checks pass: output a summary table of passing test counts and proceed to the checkpoint.
  </action>
  <verify>
    <automated>cd /c/Users/Samiul/Pictures/doctor/public_html && composer test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `composer test -- --filter AssistantPortal` exits 0 with ≥ 20 passing tests, 0 failures
    - `composer test` exits 0 (full suite green, no regressions)
    - All 6 grep checks return expected values (≥1 match each)
    - Output message confirms: "Phase 6 automated checks: ALL PASS"
  </acceptance_criteria>
  <done>Full test suite green; all automated checks pass; ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify Phase 6 conformance against running dev server</name>
  <what-built>
Phase 6 complete implementation:
- Queue desk (desk.php) rewritten with source tags, csrf_field(), onEnd auto-reorder, nowServingBadge, billing collapse, Token button, emergency btn-danger
- _billing_panel.php partial: fee input, paid/due toggle, cash/bKash toggle, bKash initiate section
- queue_ticker_json() endpoint for real-time serving badge
- bkash_initiate_desk() endpoint with auth guard (PAY-01)
- print_token() + token_print.php for serial print
- mark_queue_fee_paid_ajax() for AJAX fee recording
- emergency_bump() updated to set is_emergency=1
- Migration: is_emergency column added to chamber_serial_queue
- CSS: 3 new rule groups in chamber-practice.css
  </what-built>
  <how-to-verify>
Start the local dev server (Laragon or equivalent). Run the database migration via the CI3 migration runner or by executing the migration manually.

Then verify each checklist item:

**Queue Desk (UI-12):**
1. Navigate to `assistant_chamber/desk?doctor_id=1&chamber_id=1&date=[today]`
2. Confirm each queue row shows a Source tag (Walk-in, Phone, or Online) with correct icon
3. Confirm drag handle (grip icon) is in the first column; drag a row to reorder
4. After drag, verify toastr "Queue reordered." appears within 2 seconds (not alert())
5. Click "Save order" button in the panel HEADER (not below the table)
6. Click "Check in" on a pending row — confirm status changes to "arrived" after page reload
7. Click "Emergency" on any row — confirm the row moves to the top and shows red "emergency" tag after reload
8. Click "Token" on any row — confirm a new tab opens with a printable page showing serial number in large text; confirm print dialog auto-triggers
9. Confirm `#nowServingBadge` in the panel header updates when "Serving" is clicked (may take up to 12 seconds)

**Billing Panel (UI-13):**
10. Click "Bill" button on any row — confirm billing panel expands inline
11. Enter a fee amount (e.g. 500), confirm BDT prefix shows from settings
12. Click "Due" toggle — confirm it becomes active, Paid loses active state
13. Click "bKash" method toggle — confirm bKash section (`#bkashSection_*`) appears
14. Click "Cash" — confirm bKash section hides
15. Click "Record fee" with cash + paid — confirm toastr "Fee recorded." and panel collapses

**bKash (PAY-01):**
16. Select bKash method, enter amount, click "Initiate bKash" — if bKash sandbox is NOT configured, confirm the response is a clean JSON error ("bKash not configured for this hospital") displayed inline in red — NOT a 500 error, NOT a blank page
17. Confirm the button re-enables after the response and no redirect occurs

**Real-time (QUEUE-01):**
18. Open doctor dashboard (or patient queue screen) in a separate tab
19. Mark a patient as "Serving" on the assistant desk
20. Confirm the `#nowServingBadge` on the assistant desk updates to "Now serving: #N" within 12s
21. Confirm the patient queue screen (from Phase 4) updates "Now serving" within 15s

**CSS:**
22. Inspect a queue row with emergency status — confirm `background: #fef2f2` / `color: #b91c1c` applied
23. Inspect a source tag — confirm border-radius, font-size:12px, font-weight:bold applied
24. Inspect "Initiate bKash" button — confirm pink background `#e2136e`
  </how-to-verify>
  <resume-signal>
Type "approved" if all checklist items pass.
Type "issues: [list]" to describe any visual or functional problems found — Claude will create a gap closure plan for each issue before Phase 6 is marked complete.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev server → human tester | Local dev only; no production data at risk during verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Information Disclosure | bKash sandbox credentials in test | accept | Verification uses sandbox or unconfigured state; no live credentials during dev testing |
</threat_model>

<verification>
- `composer test -- --filter AssistantPortal` → ≥ 20 passing, 0 failures
- `composer test` → full suite green
- Human tester approves all 22 checklist items
</verification>

<success_criteria>
Phase 6 complete when: full test suite green AND human tester types "approved" confirming all 22 conformance items pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06-assistant-portal-real-time/06-05-SUMMARY.md`
</output>

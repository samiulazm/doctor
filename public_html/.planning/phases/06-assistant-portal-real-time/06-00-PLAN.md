---
phase: 06-assistant-portal-real-time
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/Unit/AssistantPortal/AssistantPortalTest.php
autonomous: true
requirements:
  - UI-12
  - UI-13
  - PAY-01
  - QUEUE-01

must_haves:
  truths:
    - "AssistantPortalTest.php exists and PHPUnit can collect it without errors"
    - "All 4 requirements have at least one failing test method that will go green when Phase 6 code lands"
    - "Full test suite still passes (no regressions introduced by scaffold)"
  artifacts:
    - path: "tests/Unit/AssistantPortal/AssistantPortalTest.php"
      provides: "Static-analysis test scaffold covering UI-12, UI-13, PAY-01, QUEUE-01"
      exports: ["AssistantPortalTest"]
      min_lines: 80
  key_links:
    - from: "tests/Unit/AssistantPortal/AssistantPortalTest.php"
      to: "application/modules/assistant_chamber/controllers/Assistant_chamber.php"
      via: "file_get_contents + assertStringContainsString"
      pattern: "Assistant_chamber\\.php"
---

<objective>
Create the PHPUnit test scaffold that drives Wave 1–3 implementation.

Purpose: Establish failing tests for all 4 Phase 6 requirements before touching any production code. Every subsequent wave must leave the full suite green.

Output: `tests/Unit/AssistantPortal/AssistantPortalTest.php` — static-analysis assertions (file content checks, method existence, CSS class presence, migration existence) covering UI-12, UI-13, PAY-01, QUEUE-01.
</objective>

<execution_context>
@C:\Users\Samiul\Pictures\doctor\public_html\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Samiul\Pictures\doctor\public_html\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Samiul\Pictures\doctor\public_html\.planning\PROJECT.md
@C:\Users\Samiul\Pictures\doctor\public_html\.planning\ROADMAP.md
@C:\Users\Samiul\Pictures\doctor\public_html\.planning\phases\06-assistant-portal-real-time\06-RESEARCH.md
@C:\Users\Samiul\Pictures\doctor\public_html\.planning\phases\06-assistant-portal-real-time\06-VALIDATION.md

<interfaces>
<!-- Existing test scaffold pattern — mirror from SecurityPatchTest.php -->
From tests/Unit/Security/SecurityPatchTest.php:
```php
namespace Tests\Unit\Security;
use PHPUnit\Framework\TestCase;
final class SecurityPatchTest extends TestCase
{
    // readSource(string $relPath): string — reads from project root
    private function readSource(string $relPath): string { ... }
}
```

<!-- Paths the new test must assert against -->
Controller: application/modules/assistant_chamber/controllers/Assistant_chamber.php
Payment:    application/modules/payment_bd/controllers/Payment_bd.php
View desk:  application/modules/assistant_chamber/views/assistant/desk.php
CSS:        common/css/chamber-practice.css (or application/assets/css/chamber-practice.css — confirm exact path from existing test patterns)
Migration:  application/migrations/20260427000012_chamber_queue_is_emergency.php
Token view: application/modules/assistant_chamber/views/assistant/token_print.php
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create AssistantPortalTest.php scaffold</name>
  <files>tests/Unit/AssistantPortal/AssistantPortalTest.php</files>
  <read_first>
    - tests/Unit/Security/SecurityPatchTest.php — read full file; extract namespace pattern, readSource() helper, file-relative path resolution, and TestCase subclass structure
    - tests/Unit/PatientPortal/ (any test file) — confirm namespace pattern for new subdirectory
    - application/modules/assistant_chamber/controllers/Assistant_chamber.php — scan first 30 lines only; confirm class name and module path
  </read_first>
  <action>
Create `tests/Unit/AssistantPortal/AssistantPortalTest.php` using namespace `Tests\Unit\AssistantPortal`.

Mirror the `readSource(string $relPath): string` helper from SecurityPatchTest.php — it resolves relative to project root using `dirname(__DIR__, 3)` (adjust depth to match SecurityPatchTest's pattern; the helper must resolve from `tests/Unit/AssistantPortal/` to repo root).

Write exactly these test methods — all will FAIL until the implementation waves run:

**UI-12 group:**
- `test_ui12_queue_ticker_json_method_exists()` — asserts `Assistant_chamber.php` contains `function queue_ticker_json`
- `test_ui12_print_token_method_exists()` — asserts `Assistant_chamber.php` contains `function print_token`
- `test_ui12_emergency_bump_sets_is_emergency()` — asserts `Assistant_chamber.php` contains `is_emergency` (the column set in the method)
- `test_ui12_desk_view_has_source_tag()` — asserts `desk.php` contains `chamber-source-tag`
- `test_ui12_desk_view_has_now_serving_badge()` — asserts `desk.php` contains `nowServingBadge`
- `test_ui12_desk_view_uses_csrf_field()` — asserts `desk.php` contains `csrf_field()` (Phase 3 standard); asserts `desk.php` does NOT contain `get_csrf_token_name()` (manual injection banned)
- `test_ui12_desk_view_has_print_token_link()` — asserts `desk.php` contains `print_token`
- `test_ui12_migration_adds_is_emergency()` — asserts migration file `20260427000012_chamber_queue_is_emergency.php` exists (use `assertFileExists`)

**UI-13 group:**
- `test_ui13_mark_queue_fee_paid_ajax_method_exists()` — asserts `Assistant_chamber.php` contains `function mark_queue_fee_paid_ajax`
- `test_ui13_billing_panel_partial_exists()` — asserts `assertFileExists` on `views/assistant/_billing_panel.php`
- `test_ui13_billing_panel_has_fee_input()` — asserts `_billing_panel.php` contains `id="fee_` (fee amount input)
- `test_ui13_billing_panel_has_paid_due_toggle()` — asserts `_billing_panel.php` contains `paidDueToggle_`
- `test_ui13_billing_panel_has_pay_method_toggle()` — asserts `_billing_panel.php` contains `payMethodToggle_`

**PAY-01 group:**
- `test_pay01_bkash_initiate_desk_method_exists()` — asserts `Payment_bd.php` contains `function bkash_initiate_desk`
- `test_pay01_bkash_initiate_desk_has_auth_guard()` — asserts `Payment_bd.php` contains `ion_auth->logged_in()` within 30 lines of `function bkash_initiate_desk`; simplest approach: assert `Payment_bd.php` contains both `bkash_initiate_desk` and `ion_auth->logged_in()` (string presence is sufficient)
- `test_pay01_billing_panel_has_bkash_section()` — asserts `_billing_panel.php` contains `bkashSection_`

**QUEUE-01 group:**
- `test_queue01_ticker_json_returns_serial_key()` — asserts `Assistant_chamber.php` contains `'serial'` adjacent to `queue_ticker_json` (string `serial` present anywhere in the controller is the proxy)
- `test_queue01_css_has_emergency_class()` — asserts `chamber-practice.css` contains `.chamber-status.emergency`
- `test_queue01_css_has_source_tag()` — asserts `chamber-practice.css` contains `.chamber-source-tag`
- `test_queue01_css_has_btn_bkash()` — asserts `chamber-practice.css` contains `.btn-bkash`
- `test_queue01_token_print_view_exists()` — asserts `assertFileExists` on `views/assistant/token_print.php`

For the CSS path: `common/css/chamber-practice.css`. Confirm the exact path works from the readSource helper; if the CSS file lives at `application/assets/css/chamber-practice.css`, use that instead. Read `common/css/chamber-practice.css` via the helper to confirm it resolves — if it returns empty, try `application/assets/css/chamber-practice.css`. Hardcode whichever resolves.

No TDD cycles needed for this scaffold task — the file is created once.
  </action>
  <verify>
    <automated>cd /c/Users/Samiul/Pictures/doctor/public_html && composer test -- --filter AssistantPortal 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Unit/AssistantPortal/AssistantPortalTest.php` exists
    - PHPUnit collects the file without parse errors: `composer test -- --filter AssistantPortal` outputs a test count ≥ 20
    - All 20+ tests FAIL (expected — implementation not yet written): output shows "FAILURES" or all tests individually skipped/failed, zero errors
    - `composer test` (full suite) exits 0 with no new errors beyond the expected AssistantPortal failures — existing passing tests remain green
    - `grep -v '^//' tests/Unit/AssistantPortal/AssistantPortalTest.php | grep -c "function test_"` returns ≥ 20
  </acceptance_criteria>
  <done>AssistantPortalTest.php collects 20+ test methods; all fail; existing suite stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → source files | Tests read production source files; no exec, no DB, no HTTP — read-only static analysis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-00-01 | Information Disclosure | test scaffold reads source paths | accept | Tests run in dev/CI only; file paths are relative to repo root, no secrets involved |
</threat_model>

<verification>
- `composer test -- --filter AssistantPortal` runs without PHP parse errors
- Output shows ≥ 20 test methods collected
- All methods fail (red) because production code not yet written — this is expected
- `composer test` full suite: all previously-passing tests still pass
</verification>

<success_criteria>
Wave 0 complete when AssistantPortalTest.php exists, collects ≥ 20 test methods, all fail as expected, and the full suite has no new regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-assistant-portal-real-time/06-00-SUMMARY.md`
</output>

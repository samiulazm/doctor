---
phase: 02-code-health
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/Unit/CodeHealth/CodeHealthTest.php
autonomous: true
requirements:
  - BUG-01
  - BUG-02
  - BUG-03
  - BUG-04
  - BUG-05
  - BUG-06

must_haves:
  truths:
    - "tests/Unit/CodeHealth/CodeHealthTest.php exists with test methods for all 6 BUG requirements"
    - "`composer test -- --filter CodeHealth` runs and reports failures (RED state — bugs not yet fixed)"
    - "All 6 BUG test methods follow the static source-file assertion pattern from SecurityPatchTest.php"
  artifacts:
    - path: "tests/Unit/CodeHealth/CodeHealthTest.php"
      provides: "Static grep assertions for BUG-01 through BUG-06"
      exports: ["test_bug01_no_full_table_scan", "test_bug02_testpkz_controller_deleted", "test_bug02_home_backup_deleted", "test_bug03_no_phpexcel_instantiation", "test_bug03_phpspreadsheet_namespace_present", "test_bug04_no_die_in_addappointment", "test_bug05_no_fopen_in_home", "test_bug05_no_fopen_in_settings", "test_bug06_no_error_reporting_suppression"]
  key_links:
    - from: "tests/Unit/CodeHealth/CodeHealthTest.php"
      to: "application/modules/home/controllers/Home.php"
      via: "readModule() file_get_contents"
      pattern: "readModule.*Home\\.php"
    - from: "tests/Unit/CodeHealth/CodeHealthTest.php"
      to: "application/modules/api/controllers/Api.php"
      via: "readModule() file_get_contents"
      pattern: "readModule.*Api\\.php"
---

<objective>
Create the Wave 0 test scaffold for Phase 2 Code Health. This is a single PHPUnit test class with static source-file assertions for all six BUG requirements (BUG-01 through BUG-06).

Purpose: All subsequent Wave 1 plans must run RED (failing) against this scaffold before their fixes are applied, then GREEN after. The scaffold drives the RED → GREEN feedback loop.

Output: `tests/Unit/CodeHealth/CodeHealthTest.php` — executable immediately, initially all tests fail (bugs still present), all tests pass after Phase 2 Wave 1+2 complete.
</objective>

<execution_context>
@C:\Users\Samiul\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Samiul\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-code-health/02-RESEARCH.md
@.planning/phases/02-code-health/02-VALIDATION.md

<interfaces>
<!-- Pattern from tests/Unit/Security/SecurityPatchTest.php (already in codebase) -->

```php
namespace Tests\Unit\Security;
use PHPUnit\Framework\TestCase;

final class SecurityPatchTest extends TestCase
{
    // Static source-file assertions — no CI3 bootstrap needed
    public function test_sec01_api_token_uses_random_bytes(): void
    {
        $src = $this->readModule('application/modules/api/controllers/Api.php');
        $this->assertStringNotContainsString('rand(1111, 9999)', $src, 'message');
        $this->assertStringContainsString('random_bytes', $src, 'message');
    }

    private function readModule(string $relativePath): string
    {
        $root = dirname(__DIR__, 3);  // from tests/Unit/Security/ up 3 = project root
        $full = $root . DIRECTORY_SEPARATOR . str_replace('/', DIRECTORY_SEPARATOR, $relativePath);
        $this->assertFileExists($full, "Source file not found: {$relativePath}");
        return (string) file_get_contents($full);
    }
}
```

Note: CodeHealthTest.php is in tests/Unit/CodeHealth/ — dirname(__DIR__, 3) is the same project root.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CodeHealthTest.php scaffold with all 6 BUG assertions</name>
  <files>tests/Unit/CodeHealth/CodeHealthTest.php</files>
  <read_first>
    - tests/Unit/Security/SecurityPatchTest.php (copy exact class structure, readModule() helper, namespace pattern)
    - application/modules/home/controllers/Home.php lines 87-110 (confirm exact string to assert absent for BUG-01)
    - application/modules/api/controllers/Api.php lines 895-910 (confirm exact die() context for BUG-04)
    - application/modules/home/controllers/Home.php lines 560-620 (confirm fopen/fwrite/rename for BUG-05)
    - application/modules/settings/controllers/Settings.php lines 1005-1060 (confirm fopen/fwrite pattern for BUG-05)
  </read_first>
  <action>
Create `tests/Unit/CodeHealth/` directory and write `CodeHealthTest.php` using exactly the same structure as `SecurityPatchTest.php`. The class must be `final class CodeHealthTest extends TestCase` in namespace `Tests\Unit\CodeHealth`.

Use `dirname(__DIR__, 3)` in `readModule()` — CodeHealthTest.php is at `tests/Unit/CodeHealth/`, so three levels up reaches the project root (same depth as SecurityPatchTest.php).

Write the following test methods (in order):

**test_bug01_no_full_table_scan** — reads `application/modules/home/controllers/Home.php`, asserts `->get('patient')->result()` is NOT present as a bare table scan. The pattern to assert absent is: `$this->db->get('patient')->result()` (without a preceding `where_in` on the same line). Use `assertStringNotContainsString` checking for the literal string `"$this->db->get('patient')->result()"`.

**test_bug02_testpkz_controller_deleted** — use `assertFileDoesNotExist` on `tests/Unit/CodeHealth/../../application/modules/testpkz/controllers/Testpkz.php` (compute from root: `$root . '/application/modules/testpkz/controllers/Testpkz.php'`).

**test_bug02_home_backup_deleted** — use `assertFileDoesNotExist` on `application/modules/home/views/home_backup.php`.

**test_bug02_testpkz_removed_from_required** — reads `application/hooks/required.php`, asserts the string `"'testpkz'"` is NOT present (the entry in the `$common` array).

**test_bug03_no_phpexcel_instantiation** — reads `application/modules/import/controllers/Import.php`, asserts `PHPExcel_IOFactory` is NOT present and `PHPExcel_Cell` is NOT present.

**test_bug03_phpspreadsheet_namespace_present** — reads `application/modules/import/controllers/Import.php`, asserts `PhpOffice\PhpSpreadsheet` IS present (minimum 3 occurrences using `substr_count`).

**test_bug04_no_die_in_addappointment** — reads `application/modules/api/controllers/Api.php`. Extract the body of `addAppointment()` function from the source (from `function addAppointment` to the next `function ` at the same indent level — use `substr` between those positions). Assert `die()` is NOT present in that extracted substring.

**test_bug05_no_fopen_in_home** — reads `application/modules/home/controllers/Home.php`, asserts `fopen('index` is NOT present and `fopen('index.tmp` is NOT present.

**test_bug05_no_fopen_in_settings** — reads `application/modules/settings/controllers/Settings.php`, asserts `fopen(` is NOT present (the file-write block uses fopen).

**test_bug06_no_error_reporting_in_bed** — reads `application/modules/bed/controllers/Bed.php`, asserts `error_reporting(0)` is NOT present.

**test_bug06_no_error_reporting_in_finance** — reads `application/modules/finance/controllers/Finance.php`, asserts `error_reporting(0)` is NOT present.

**test_bug06_no_error_reporting_in_patient** — reads `application/modules/patient/controllers/Patient.php`, asserts `error_reporting(0)` is NOT present.

**test_bug06_no_error_reporting_in_payroll** — reads `application/modules/payroll/controllers/Payroll.php`, asserts `error_reporting(0)` is NOT present.

**test_bug06_no_error_reporting_in_prescription** — reads `application/modules/prescription/controllers/Prescription.php`, asserts `error_reporting(0)` is NOT present.

**test_bug06_no_error_reporting_in_settings** — reads `application/modules/settings/controllers/Settings.php`, asserts `error_reporting(0)` is NOT present.

Each assertion message must follow the format: `'BUG-XX: [pattern] still present in [File.php]'`.

Copy the `readModule()` private helper verbatim from SecurityPatchTest.php — no changes except the namespace.
  </action>
  <verify>
    <automated>composer test -- --filter CodeHealth 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Unit/CodeHealth/CodeHealthTest.php` exists
    - `grep -c "function test_bug" tests/Unit/CodeHealth/CodeHealthTest.php` outputs `15` (15 test methods)
    - `grep -c "BUG-0" tests/Unit/CodeHealth/CodeHealthTest.php` outputs at least `15` (each method references its requirement ID in the assertion message)
    - `grep -c "readModule" tests/Unit/CodeHealth/CodeHealthTest.php` outputs at least `12` (one per file-reading test)
    - `composer test -- --filter CodeHealth` exits non-zero (tests FAIL — bugs not yet fixed; this is the intended RED state)
    - `composer test -- --filter CodeHealth 2>&amp;1 | grep -c "FAILURES\|ERRORS"` outputs `1` (test runner reports failures or errors)
    - `php -l tests/Unit/CodeHealth/CodeHealthTest.php` exits 0 (valid PHP syntax)
  </acceptance_criteria>
  <done>CodeHealthTest.php exists, parses cleanly, runs under PHPUnit, and fails in RED state because the bugs it guards against are still present in the source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → source files | PHPUnit reads production source files via file_get_contents; no eval or execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00-01 | Information Disclosure | CodeHealthTest.php | accept | Test file contains only file paths and string patterns — no secrets, no credentials. Low risk. |
</threat_model>

<verification>
Run after task completes:

```bash
composer test -- --filter CodeHealth
```

Expected: Tests run, all FAIL (RED state — bugs not yet applied). Exit code non-zero.

```bash
grep -c "function test_bug" tests/Unit/CodeHealth/CodeHealthTest.php
```

Expected: `15`
</verification>

<success_criteria>
- `tests/Unit/CodeHealth/CodeHealthTest.php` exists and is syntactically valid PHP
- PHPUnit discovers and runs all 15 test methods under the `CodeHealth` filter
- All tests FAIL (RED) — confirms assertions are correctly targeted at the live bugs
- Test class follows identical structure to SecurityPatchTest.php (same readModule helper, same assertion style)
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-health/02-00-SUMMARY.md`
</output>

---
phase: 03-admin-design-system
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
files_modified:
  - application/modules/appointment/views/appointment.php
  - application/modules/doctor/views/doctor.php
  - application/modules/patient/views/patient.php
  - application/modules/lab/views/lab.php
  - application/modules/prescription/views/prescription.php
  - application/modules/finance/views/finance.php
  - application/modules/medicine/views/medicine.php
  - application/modules/emergency/views/emergency.php
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "All 8 priority module list views use content-wrapper bg-light (not bg-gradient-light)"
    - "All 8 views use partials/page_header via $CI->load->view() (not inline content-header HTML)"
    - "All 8 views wrap their content in .card.shadow-sm.border-0 structure"
    - "No <div style=...> layout sections exist in these 8 views"
    - "lab.php is the smoke-test module — verified manually after this plan before bulk migration"
  artifacts:
    - path: "application/modules/appointment/views/appointment.php"
      provides: "Conforming AdminLTE 3 card layout with page_header partial"
      contains: "card shadow-sm border-0"
    - path: "application/modules/lab/views/lab.php"
      provides: "Non-conforming → conforming migration (smoke-test case)"
      contains: "partials/page_header"
  key_links:
    - from: "application/modules/*/views/*.php"
      to: "application/views/partials/page_header.php"
      via: "$CI->load->view('partials/page_header', [...])"
      pattern: "load->view\\('partials/page_header'"
---

<objective>
Migrate the 8 highest-traffic module list views to the standard AdminLTE 3 card layout.

`lab.php` is the primary smoke-test case (confirmed non-conforming: uses bg-gradient-light +
inline section header). Fixing it first validates that $CI->load->view('partials/page_header')
resolves correctly across HMVC modules before bulk migration in Plan 06.

Purpose: Ensure the most-used admin pages match the design contract. Doctors, assistants, and
admins load these pages dozens of times per day.
Output: 8 view files updated to the standard card layout from 03-UI-SPEC.md §Card/Section Layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-admin-design-system/03-UI-SPEC.md
@.planning/phases/03-admin-design-system/03-RESEARCH.md
@.planning/phases/03-admin-design-system/03-01-SUMMARY.md

<interfaces>
<!-- Conforming layout shell (source: 03-UI-SPEC.md §Card/Section Layout): -->
```php
<div class="content-wrapper bg-light">
    <?php $CI = get_instance(); $CI->load->view('partials/page_header', [
        'title'       => lang('MODULE_NAME'),
        'icon'        => 'fas fa-ICON_NAME text-primary mr-2',
        'breadcrumbs' => [
            ['label' => lang('home'),        'url' => 'home'],
            ['label' => lang('MODULE_NAME'), 'url' => null],
        ],
    ]); ?>
    <section class="content py-4">
        <div class="container-fluid">
            <div class="row justify-content-center">
                <div class="col-12">
                    <div class="card shadow-sm border-0">
                        <div class="card-header bg-white border-bottom d-flex flex-wrap align-items-center justify-content-between gap-2 py-3">
                            <h3 class="card-title h6 mb-0 text-muted text-uppercase">
                                <?php echo lang('LIST_LABEL'); ?>
                            </h3>
                            <button type="button" class="btn btn-sm btn-primary"
                                    data-toggle="modal" data-target="#addModal">
                                <i class="fas fa-plus mr-1"></i>
                                <?php echo lang('add_new'); ?>
                            </button>
                        </div>
                        <div class="card-body p-4">
                            <div class="custom_buttons mb-3"></div>
                            <div class="table-responsive">
                                <!-- table goes here -->
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>
```

<!-- Non-conforming pattern to replace (source: 03-RESEARCH.md confirmed in lab.php): -->
<!-- <div class="content-wrapper bg-gradient-light">  → change to bg-light -->
<!-- <section class="content-header py-4 bg-white shadow-sm">  → remove, use page_header partial -->
<!-- <h1 class="display-4 font-weight-black mb-0">...</h1>  → remove, page_header partial provides heading -->

<!-- CRITICAL path rule (source: 03-RESEARCH.md Pitfall 3): -->
<!-- Always use $CI = get_instance(); then $CI->load->view('partials/page_header', [...]) -->
<!-- NEVER $this->load->view('partials/page_header') from inside a view file -->
<!-- Existing conforming views (appointment.php, patient.php) already use this pattern — follow them -->

<!-- If a view already has $CI = get_instance() earlier in the file, reuse that variable -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix lab.php layout (smoke-test case) — non-conforming to conforming</name>
  <read_first>
    - application/modules/lab/views/lab.php (full file — confirmed non-conforming)
    - application/modules/appointment/views/appointment.php (conforming reference — read for exact $CI pattern)
    - application/views/partials/page_header.php (read to understand expected $data keys)
  </read_first>
  <files>application/modules/lab/views/lab.php</files>
  <action>
Read the full current content of `application/modules/lab/views/lab.php`.

Identify the non-conforming layout sections:
1. The opening wrapper: `<div class="content-wrapper bg-gradient-light">` — change `bg-gradient-light` to `bg-light`
2. The inline section header block starting with `<section class="content-header py-4 bg-white shadow-sm">`:
   - Remove this entire section (from `<section class="content-header...">` to its closing `</section>`)
   - Replace it with the `$CI->load->view('partials/page_header', [...])` call, using the lab module's title and icon
   - Use `lang('lab')` or `lang('lab_test')` for title (check what lang keys exist in the file already)
   - Icon: `fas fa-flask text-primary mr-2` (appropriate for lab module)
   - Breadcrumbs: home → lab

3. The content section: find `<section class="content...">` (the main content section, not the header)
   - If it does not have `py-4`, add it: `<section class="content py-4">`
   - Ensure the content is wrapped in `.container-fluid > .row.justify-content-center > .col-12`

4. The table/list area: wrap in `.card.shadow-sm.border-0 > .card-header > .card-body.p-4` if not already
   - Preserve ALL existing content (modals, forms, table data, JavaScript) inside the new card structure

5. Ensure `<div class="custom_buttons mb-3"></div>` exists inside `.card-body` before the table

Preserve ALL existing PHP logic, modals, JavaScript, and table content — only restructure the
outer layout HTML shell.

Write the updated file. Verify no PHP syntax errors.
  </action>
  <verify>
    <automated>php -l /c/Users/Samiul/Pictures/doctor/public_html/application/modules/lab/views/lab.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bg-gradient-light" application/modules/lab/views/lab.php` returns 0
    - `grep -c "partials/page_header" application/modules/lab/views/lab.php` returns 1
    - `grep -c "card shadow-sm border-0" application/modules/lab/views/lab.php` returns at least 1
    - `grep -c "content-header py-4 bg-white shadow-sm" application/modules/lab/views/lab.php` returns 0
    - `php -l application/modules/lab/views/lab.php` exits 0
  </acceptance_criteria>
  <done>lab.php migrated to standard card layout with partials/page_header. Smoke test ready for manual verification.</done>
</task>

<task type="auto">
  <name>Task 2: Apply standard card layout to 7 remaining priority modules</name>
  <read_first>
    - application/modules/appointment/views/appointment.php (conforming reference — note any non-standard additions to preserve)
    - application/modules/doctor/views/doctor.php
    - application/modules/patient/views/patient.php
    - application/modules/prescription/views/prescription.php
    - application/modules/finance/views/finance.php
    - application/modules/medicine/views/medicine.php
    - application/modules/emergency/views/emergency.php
  </read_first>
  <files>
    application/modules/doctor/views/doctor.php
    application/modules/patient/views/patient.php
    application/modules/prescription/views/prescription.php
    application/modules/finance/views/finance.php
    application/modules/medicine/views/medicine.php
    application/modules/emergency/views/emergency.php
  </files>
  <action>
For each of the 6 non-lab priority modules, read the current view file and apply the same
layout migration as Task 1:

**For each file:**

1. Check conformance status first:
   - Already conforming (uses bg-light + partials/page_header + .card.shadow-sm.border-0)? → skip file, note in summary
   - Partially conforming (has card but wrong wrapper or no partial)? → apply specific fixes
   - Non-conforming (bg-gradient-light or inline content-header section)? → full shell replacement

2. For non-conforming files, apply the same three-step fix from Task 1:
   a. Change wrapper class to `content-wrapper bg-light`
   b. Replace inline content-header `<section>` with `$CI->load->view('partials/page_header', [...])`
   c. Wrap content in `.card.shadow-sm.border-0 > .card-header + .card-body.p-4` if not present

3. Module-specific icons to use for `partials/page_header`:
   - doctor: `fas fa-user-md text-primary mr-2`
   - patient: `fas fa-procedures text-primary mr-2`
   - prescription: `fas fa-prescription-bottle-alt text-primary mr-2`
   - finance: `fas fa-money-bill-wave text-primary mr-2`
   - medicine: `fas fa-pills text-primary mr-2`
   - emergency: `fas fa-ambulance text-primary mr-2`

4. For lang() keys — use the same keys already present in the file's existing content-header title.
   Do not invent new lang keys; use whatever `lang()` call the file already has for its title.

5. Note: appointment.php is already conforming per research audit — DO NOT modify it.

Preserve ALL existing content: modals, forms, JS, table structure. Only the outer layout shell changes.
Write each modified file. Verify PHP syntax.
  </action>
  <verify>
    <automated>grep -rn "bg-gradient-light" /c/Users/Samiul/Pictures/doctor/public_html/application/modules/doctor/views/ /c/Users/Samiul/Pictures/doctor/public_html/application/modules/patient/views/ /c/Users/Samiul/Pictures/doctor/public_html/application/modules/prescription/views/ /c/Users/Samiul/Pictures/doctor/public_html/application/modules/finance/views/ /c/Users/Samiul/Pictures/doctor/public_html/application/modules/medicine/views/ /c/Users/Samiul/Pictures/doctor/public_html/application/modules/emergency/views/ --include="*.php" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "bg-gradient-light" application/modules/{doctor,patient,prescription,finance,medicine,emergency}/views/` returns 0
    - `grep -rn "partials/page_header" application/modules/{doctor,patient,prescription,finance,medicine,emergency}/views/ --include="*.php" | wc -l` ≥ 6
    - `php -l` on each modified file exits 0
    - `composer test -- --filter test_ui01_no_bg_gradient_light_in_list_views` progresses toward GREEN (fewer failures)
  </acceptance_criteria>
  <done>All 7 priority module views (plus lab from Task 1) use standard card layout. 8 high-traffic modules conformant.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    lab.php migrated from bg-gradient-light + inline section header to the standard AdminLTE 3 card
    layout with partials/page_header partial. This is the HMVC path resolution smoke test.
  </what-built>
  <how-to-verify>
    1. Load the admin lab page in your browser: http://localhost/lab (or your local dev URL for the lab module)
    2. Verify the page title "Lab" (or equivalent) appears in the standard page header breadcrumb bar
    3. Verify the breadcrumb shows: Home > Lab
    4. Verify the page content is inside a card with a white header and shadow
    5. Verify no "Unable to load the requested file: partials/page_header.php" error appears
    6. Open DevTools → Network tab, reload page, confirm no 500 errors
    7. Repeat steps 1-5 for the doctor page (/doctor) and patient page (/patient)
  </how-to-verify>
  <resume-signal>Type "approved" if all 3 pages render correctly, or describe any errors you see</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view → partials/page_header | CI3 HMVC $CI->load->view() cross-module path resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | XSS in page_header title/breadcrumb | accept | partials/page_header uses lang() keys — i18n strings, not user input |
| T-03-03-02 | Denial | HMVC view path resolution failure (Pitfall 3) | mitigate | Use $CI = get_instance(); $CI->load->view(...) pattern exclusively; smoke-test on lab.php before bulk migration |
</threat_model>

<verification>
```bash
# UI-01 layout test progress:
cd /c/Users/Samiul/Pictures/doctor/public_html
composer test -- --filter test_ui01_no_bg_gradient_light_in_list_views

# Manual smoke test:
# Load /lab, /doctor, /patient in browser — all must render without PHP errors
```
</verification>

<success_criteria>
- lab.php, doctor.php, patient.php, prescription.php, finance.php, medicine.php, emergency.php all use bg-light wrapper
- All 8 priority files use partials/page_header via $CI->load->view()
- All 8 files have .card.shadow-sm.border-0 structure
- Human verify checkpoint passes (lab page renders correctly in browser)
- No PHP syntax errors in any modified file
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-design-system/03-03-SUMMARY.md`
</output>

---
phase: 04-patient-portal
plan: "02"
type: execute
wave: 1
depends_on:
  - "04-00"
files_modified:
  - application/modules/portal/controllers/Portal.php
  - application/modules/portal/views/portal/landing.php
autonomous: true
requirements:
  - AUTH-01

must_haves:
  truths:
    - "OTP request AJAX (portal/request_otp) is wired from the landing page with CSRF rotation"
    - "OTP verify AJAX (portal/verify_otp) is wired from the landing page with CSRF rotation"
    - "OTP send button is disabled while AJAX is in-flight"
    - "OTP field only appears after r.ok === true from request_otp"
    - "60-second resend countdown and Resend OTP link are implemented"
    - "Verified state shows .chamber-status.arrived badge with text Verified"
    - "Error messages from r.msg are shown in #otpMsg as .text-danger"
    - "ticker_json CSRF rotation: JSON response includes csrf key on every response"
  artifacts:
    - path: "application/modules/portal/views/portal/landing.php"
      provides: "OTP modal UI — inline in landing page, session-gated trigger"
      contains: "chamber-otp-input|sendOtp|verifyOtp|resendTimer"
    - path: "application/modules/portal/controllers/Portal.php"
      provides: "request_otp and verify_otp return csrf in JSON response"
      contains: "csrf.*get_csrf_hash"
  key_links:
    - from: "landing.php OTP JS (sendOtp)"
      to: "portal/request_otp"
      via: "$.post with CSRF token; reads r.csrf for rotation"
      pattern: "request_otp"
    - from: "landing.php OTP JS (verifyOtp)"
      to: "portal/verify_otp"
      via: "$.post with CSRF token; reads r.csrf for rotation"
      pattern: "verify_otp"
---

<objective>
Wire the OTP auth flow inline in `landing.php` (AUTH-01). The existing `request_otp` and `verify_otp` controller methods are fully functional — this plan adds the client-side HTML/JS so patients can trigger the OTP flow from the landing page. Also ensures `request_otp` and `verify_otp` return the updated `csrf` hash in every JSON response to handle token rotation.

Purpose: AUTH-01 — patient can log in using only a mobile phone number.
Output: OTP UI added to `landing.php`; `request_otp` and `verify_otp` append `csrf` key to their JSON responses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-patient-portal/04-UI-SPEC.md
@.planning/phases/04-patient-portal/04-RESEARCH.md

<interfaces>
From application/modules/portal/controllers/Portal.php:

request_otp() (lines 74–103):
- POST method guard
- Reads: 'slug', 'mobile' (stripped of non-digits via preg_replace)
- Rate-limit check: countRecentOtpRequests($hospital_id, $mobile, $since_ts) ≥ 6 → returns {ok:false, msg:"Too many..."}
- On success: returns json {ok:true}
- MISSING: does NOT currently return csrf hash — must be added

verify_otp() (lines 104–138):
- POST method guard
- Reads: 'slug', 'mobile', 'otp'
- On success: sets session portal_verified_phone, portal_verified_hospital
- Returns: json {ok:true} or {ok:false, msg:...}
- MISSING: does NOT currently return csrf hash — must be added

Existing triage.php CSRF pattern (Pattern 2 from RESEARCH.md):
```javascript
var csrfName = '<?php echo $this->security->get_csrf_token_name(); ?>';
var csrfHash = '<?php echo $this->security->get_csrf_hash(); ?>';
payload[csrfName] = csrfHash;
// on response: if (r.csrf) { csrfHash = r.csrf; }
```

Chamber_otp library (loaded as $this->chamber_otp):
- Already handles hash/verify — no changes needed to library

04-UI-SPEC.md OTP Modal Contract:
- Mobile: type="tel" inputmode="numeric" pattern="[0-9+\-\s]+" autocomplete="tel"
- OTP single input: type="text" inputmode="numeric" maxlength="6" pattern="[0-9]{6}" autocomplete="one-time-code"
- Error container: #otpMsg with class .text-danger
- Verified badge: .chamber-status.arrived text "Verified"
- Resend: #resendTimer showing "Resend in {N}s"; after countdown show "Resend OTP" link
- Button loading: disabled + spinner HTML while in-flight, restored in .always()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add csrf key to request_otp and verify_otp JSON responses</name>
  <files>application/modules/portal/controllers/Portal.php</files>
  <read_first>
    - application/modules/portal/controllers/Portal.php lines 74–145 (request_otp + verify_otp — find every json_encode call)
  </read_first>
  <behavior>
    - request_otp() JSON response includes 'csrf' => $this->security->get_csrf_hash() on both success and failure paths
    - verify_otp() JSON response includes 'csrf' => $this->security->get_csrf_hash() on both success and failure paths
  </behavior>
  <action>
In `Portal::request_otp()`, find every `json_encode(array(...))` call that is the final output. Add `'csrf' => $this->security->get_csrf_hash()` to each array. There are at minimum two paths: the error (rate-limit / validation fail) path and the success path.

In `Portal::verify_otp()`, do the same: add `'csrf' => $this->security->get_csrf_hash()` to every output array in json_encode calls.

Do NOT change logic, validation, or session behaviour — only add the csrf key to the response objects.
  </action>
  <verify>
    <automated>grep -c "get_csrf_hash" application/modules/portal/controllers/Portal.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "get_csrf_hash" application/modules/portal/controllers/Portal.php` outputs ≥ 4 (at least 2 per method × 2 methods)
    - `composer test -- --filter PatientPortal` exits 0
  </acceptance_criteria>
  <done>Both OTP controller methods return the updated CSRF hash on every response path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add OTP UI and JS to landing.php</name>
  <files>application/modules/portal/views/portal/landing.php</files>
  <read_first>
    - application/modules/portal/views/portal/landing.php (full read — understand current structure, find where the "Book serial now" CTA is, identify end of .chamber-public-grid, locate any existing script block)
    - application/modules/portal/views/portal/triage.php lines 90–140 (existing CSRF + OTP AJAX pattern — copy structure exactly)
    - .planning/phases/04-patient-portal/04-UI-SPEC.md (OTP Modal Contract section + Copywriting Contract)
    - .planning/phases/04-patient-portal/04-RESEARCH.md (Pattern 2: Portal CSRF on AJAX POST + Code Example: OTP AJAX with CSRF rotation)
  </read_first>
  <behavior>
    - landing.php has a "Verify phone" / "Log in with OTP" trigger button visible to unauthenticated users
    - OTP section (mobile input + Send OTP button) is inline in the page (not a Bootstrap modal)
    - Send OTP button: type="button" id="btnSendOtp", disabled during AJAX, loading spinner while in-flight
    - OTP input (id="otpInput") hidden until r.ok === true from request_otp
    - Verify OTP button (id="btnVerifyOtp") hidden until OTP input is visible; disabled during AJAX
    - #otpMsg element shows server error messages in .text-danger
    - Resend countdown 60s implemented; "Resend OTP" link visible after countdown
    - On verify_otp success: show .chamber-status.arrived "Verified" badge, hide OTP section, reload page after 1s (to show session-gated panels)
    - CSRF: var csrfName / csrfHash initialized from PHP; updated via r.csrf on every response
  </behavior>
  <action>
**Part A — HTML additions to landing.php:**

Find the section that shows the "Book serial now" CTA (inside `.chamber-public-hero` or near top of grid). If `portal_verified_phone` session is NOT set (`<?php if (empty($verified)): ?>`), show the OTP login section below or near the CTA:

```html
<?php if (empty($verified)): ?>
<div class="chamber-public-panel mt-3" id="otpLoginPanel">
    <h2 class="chamber-panel-title mb-3">
        <i class="fas fa-mobile-alt mr-1 text-primary"></i> Verify your phone to access bookings
    </h2>
    <div class="form-group">
        <label for="otpMobile" class="font-weight-bold">Mobile number</label>
        <input type="tel" id="otpMobile" class="form-control"
               inputmode="numeric" pattern="[0-9+\-\s]+"
               autocomplete="tel" placeholder="e.g. 01712345678">
    </div>
    <button type="button" id="btnSendOtp" class="btn btn-primary btn-block">Send OTP</button>
    <div id="otpMsg" class="mt-2 small"></div>

    <div id="otpVerifySection" style="display:none;" class="mt-3">
        <div class="form-group">
            <label for="otpInput" class="font-weight-bold">Enter 6-digit OTP</label>
            <input type="text" id="otpInput" class="form-control chamber-otp-input"
                   inputmode="numeric" maxlength="6" pattern="[0-9]{6}"
                   autocomplete="one-time-code" placeholder="------">
        </div>
        <button type="button" id="btnVerifyOtp" class="btn btn-success btn-block">Verify OTP</button>
        <div class="mt-2 small text-muted">
            <span id="resendTimer">Resend in 60s</span>
            <a href="#" id="resendLink" style="display:none;">Resend OTP</a>
        </div>
    </div>

    <div id="otpVerifiedBadge" style="display:none;" class="mt-3 text-center">
        <span class="chamber-status arrived">Verified</span>
    </div>
</div>
<?php endif; ?>
```

**Part B — JavaScript (add in the script block at the bottom of the view, before `</script>`):**

Use exact pattern from RESEARCH.md Pattern 2 and Code Example. Key points:
- `var csrfName = '<?php echo $this->security->get_csrf_token_name(); ?>';`
- `var csrfHash = '<?php echo $this->security->get_csrf_hash(); ?>';`
- `var slug = '<?php echo htmlspecialchars($slug, ENT_QUOTES); ?>';`
- Send OTP: POST to `'<?php echo site_url('portal/request_otp'); ?>'`
  - payload: `{slug: slug, mobile: $('#otpMobile').val(), [csrfName]: csrfHash}`
  - on r.csrf: update csrfHash
  - on r.ok: show #otpVerifySection, start 60s countdown
  - on !r.ok: show r.msg in #otpMsg as .text-danger
  - .always(): restore button from spinner to 'Send OTP'
- 60s countdown: `setInterval` decrements counter; at 0 show #resendLink, hide #resendTimer span
- Resend OTP: same as Send OTP click, re-runs the POST
- Verify OTP: POST to `'<?php echo site_url('portal/verify_otp'); ?>'`
  - payload: `{slug: slug, mobile: $('#otpMobile').val(), otp: $('#otpInput').val(), [csrfName]: csrfHash}`
  - on r.csrf: update csrfHash
  - on r.ok: show #otpVerifiedBadge, hide #otpVerifySection, after 1000ms call `window.location.reload()`
  - on !r.ok: show r.msg in #otpMsg as .text-danger; restore button
  - button disabled during in-flight: `$('#btnVerifyOtp').prop('disabled', true).html('<i class="fas fa-spinner fa-spin mr-1"></i> Verifying...')`
  - .always(): restore to 'Verify OTP'

Exact copy strings from Copywriting Contract:
- Send button: "Send OTP"
- Verify button: "Verify OTP"
- Resend: "Resend OTP"
- Countdown: "Resend in {N}s"
- Error OTP expired: "This code has expired. Please request a new OTP."
- Error OTP wrong: "Incorrect code. Please try again."
- Error too many attempts: "Too many attempts. Please request a new OTP."
- Error rate limit: "Too many requests. Please wait a few minutes and try again."
  </action>
  <verify>
    <automated>grep -v "^[ \t]*//" application/modules/portal/views/portal/landing.php | grep -c "chamber-otp-input\|sendOtp\|btnSendOtp\|verifyOtp\|btnVerifyOtp\|resendTimer"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "chamber-otp-input" application/modules/portal/views/portal/landing.php` outputs ≥ 1
    - `grep -c "request_otp\|verify_otp" application/modules/portal/views/portal/landing.php` outputs ≥ 2
    - `grep -c "resendTimer\|resendLink" application/modules/portal/views/portal/landing.php` outputs ≥ 2
    - `grep -c "csrfHash = r.csrf\|if (r.csrf)" application/modules/portal/views/portal/landing.php` outputs ≥ 2
    - `grep -c "one-time-code\|inputmode" application/modules/portal/views/portal/landing.php` outputs ≥ 2
    - `composer test -- --filter PatientPortal` exits 0
  </acceptance_criteria>
  <done>AUTH-01 OTP login flow is wired in landing.php. Patient can enter mobile, receive OTP, verify it, and the page reloads to show session-gated panels. CSRF tokens rotate correctly on every round-trip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → portal/request_otp | POST with mobile number + CSRF token |
| Browser → portal/verify_otp | POST with OTP code + CSRF token |
| PHP session → client JS | session key portal_verified_phone is never sent to client; only page reload reveals session state via server-rendered HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Elevation of Privilege | OTP brute-force | mitigate | 8-attempt lock per OTP row already in verify_otp(); 6 OTP/15min rate limit already in request_otp() |
| T-04-02-02 | Tampering | CSRF on OTP POST | mitigate | Inline CSRF token sent with every $.post; r.csrf rotates hash client-side; portal does not use admin csrf_inject.php prefilter |
| T-04-02-03 | Information Disclosure | OTP value in logs | accept | Dev-only log_message('info',...) path; never logged in ENVIRONMENT=production; existing Portal.php pattern |
| T-04-02-04 | Tampering | mobile number manipulation | mitigate | preg_replace('/\D+/', '', ...) strips non-digits in request_otp(); length enforced server-side (existing) |
</threat_model>

<verification>
- `grep -c "get_csrf_hash" application/modules/portal/controllers/Portal.php` outputs ≥ 4
- `grep -c "chamber-otp-input\|request_otp\|verify_otp" application/modules/portal/views/portal/landing.php` outputs ≥ 3
- `composer test -- --filter PatientPortal` exits 0
</verification>

<success_criteria>
AUTH-01 is complete. The landing page provides an inline OTP login flow. CSRF tokens rotate on every response. Rate-limit and attempt-lock errors display with exact copy strings from the Copywriting Contract.
</success_criteria>

<output>
After completion, create `.planning/phases/04-patient-portal/04-02-SUMMARY.md`
</output>

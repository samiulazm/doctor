---
phase: 05-doctor-portal
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/Unit/DoctorPortal/DoctorPortalTest.php
autonomous: true
requirements:
  - UI-09
  - UI-10
  - UI-11

must_haves:
  truths:
    - "PHPUnit test class DoctorPortalTest exists with @group DoctorPortal annotation"
    - "test_ui09_stat_cards_count passes when dashboard view has 4 chamber-stat-value elements"
    - "test_ui09_today_revenue_query passes when controller has payment SUM query"
    - "test_ui10_queue_json_method_exists passes when controller defines queue_json()"
    - "test_ui10_chart_data_json_method_exists passes when controller defines chart_data_json()"
    - "test_ui10_live_queue_body passes when dashboard view has empty #liveQueueBody tbody"
    - "test_ui11_search_json_method_exists passes when controller defines search_json()"
    - "test_ui11_rxframe passes when consultation_room view has id=\"rxFrame\""
  artifacts:
    - path: "tests/Unit/DoctorPortal/DoctorPortalTest.php"
      provides: "All 8 DoctorPortal test method stubs with @group DoctorPortal"
      min_lines: 60
  key_links:
    - from: "tests/Unit/DoctorPortal/DoctorPortalTest.php"
      to: "application/modules/doctor_chamber/controllers/Doctor_chamber.php"
      via: "file_get_contents static analysis + PHP token check"
      pattern: "file_get_contents.*Doctor_chamber\\.php"
---

<objective>
Create the PHPUnit test scaffold for Phase 5 — all 8 test methods as failing RED stubs that grep/static-analyse the controller and views. No mocking of the database; tests are static file analysis only.

Purpose: Establish the Nyquist-compliant feedback loop before any implementation begins. Every subsequent plan runs `composer test -- --filter DoctorPortal` after each task to catch regressions.
Output: tests/Unit/DoctorPortal/DoctorPortalTest.php with 8 failing tests grouped under @group DoctorPortal.
</objective>

<execution_context>
@/c/Users/Samiul/Pictures/doctor/public_html/.planning/../../../get-shit-done/workflows/execute-plan.md
@/c/Users/Samiul/Pictures/doctor/public_html/.planning/../../../get-shit-done/templates/summary.md
</execution_context>

<context>
@/c/Users/Samiul/Pictures/doctor/public_html/.planning/PROJECT.md
@/c/Users/Samiul/Pictures/doctor/public_html/.planning/ROADMAP.md
@/c/Users/Samiul/Pictures/doctor/public_html/.planning/STATE.md
@/c/Users/Samiul/Pictures/doctor/public_html/.planning/phases/05-doctor-portal/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DoctorPortalTest.php scaffold with 8 failing test methods</name>
  <files>tests/Unit/DoctorPortal/DoctorPortalTest.php</files>
  <read_first>
    - /c/Users/Samiul/Pictures/doctor/public_html/phpunit.xml.dist — confirm bootstrap path and test suite config
    - /c/Users/Samiul/Pictures/doctor/public_html/tests/Unit/ — check existing test structure for namespace/extends pattern to match
  </read_first>
  <action>
Create directory `tests/Unit/DoctorPortal/` if it does not exist, then create `DoctorPortalTest.php`.

The class must:
- Extend `PHPUnit\Framework\TestCase`
- Have `@group DoctorPortal` doc-comment at class level
- Define the two file path constants as class properties:
  ```php
  private string $controllerFile = __DIR__ . '/../../../application/modules/doctor_chamber/controllers/Doctor_chamber.php';
  private string $dashboardView   = __DIR__ . '/../../../application/modules/doctor_chamber/views/doctor/dashboard.php';
  private string $consultView     = __DIR__ . '/../../../application/modules/doctor_chamber/views/doctor/consultation_room.php';
  ```

Implement exactly these 8 test methods (all use `file_get_contents` static analysis — no DB, no CI bootstrap):

```php
/** @test */
public function test_ui09_stat_cards_count(): void
{
    $html = file_get_contents($this->dashboardView);
    $this->assertGreaterThanOrEqual(
        4,
        substr_count($html, 'chamber-stat-value'),
        'dashboard.php must have at least 4 .chamber-stat-value elements'
    );
}

/** @test */
public function test_ui09_today_revenue_query(): void
{
    $src = file_get_contents($this->controllerFile);
    $this->assertMatchesRegularExpression(
        '/today_revenue|SUM.*doctor_amount/s',
        $src,
        'Doctor_chamber.php must contain $today_revenue or SUM(doctor_amount) query'
    );
}

/** @test */
public function test_ui10_queue_json_method_exists(): void
{
    $src = file_get_contents($this->controllerFile);
    $this->assertStringContainsString(
        'function queue_json',
        $src,
        'Doctor_chamber.php must define queue_json()'
    );
}

/** @test */
public function test_ui10_chart_data_json_method_exists(): void
{
    $src = file_get_contents($this->controllerFile);
    $this->assertStringContainsString(
        'function chart_data_json',
        $src,
        'Doctor_chamber.php must define chart_data_json()'
    );
}

/** @test */
public function test_ui10_live_queue_body(): void
{
    $html = file_get_contents($this->dashboardView);
    $this->assertStringContainsString(
        'liveQueueBody',
        $html,
        'dashboard.php must contain tbody#liveQueueBody for long-poll population'
    );
}

/** @test */
public function test_ui10_no_server_rendered_queue_loop(): void
{
    $html = file_get_contents($this->dashboardView);
    $this->assertStringNotContainsString(
        'foreach ($queue_today',
        $html,
        'dashboard.php must NOT have a PHP foreach($queue_today) loop — queue is JS long-poll only'
    );
}

/** @test */
public function test_ui11_search_json_method_exists(): void
{
    $src = file_get_contents($this->controllerFile);
    $this->assertStringContainsString(
        'function search_json',
        $src,
        'Doctor_chamber.php must define search_json()'
    );
}

/** @test */
public function test_ui11_rxframe(): void
{
    $html = file_get_contents($this->consultView);
    $this->assertStringContainsString(
        'id="rxFrame"',
        $html,
        'consultation_room.php must have an iframe with id="rxFrame"'
    );
}
```

All 8 tests MUST FAIL on the current codebase (pre-implementation). Confirm this by noting which assertions will fail against the current files before writing.
  </action>
  <verify>
    <automated>cd /c/Users/Samiul/Pictures/doctor/public_html && composer test -- --filter DoctorPortal 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Unit/DoctorPortal/DoctorPortalTest.php` exists
    - `grep -c "@group DoctorPortal" tests/Unit/DoctorPortal/DoctorPortalTest.php` returns 1
    - `grep -c "public function test_" tests/Unit/DoctorPortal/DoctorPortalTest.php` returns 8
    - `composer test -- --filter DoctorPortal` runs without PHP syntax errors (failures are expected — tests are RED stubs)
  </acceptance_criteria>
  <done>DoctorPortalTest.php exists with 8 test methods; PHPUnit reports the test class as runnable (failures are expected pre-implementation)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → source files | Tests read production files via file_get_contents; no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-00-01 | Information Disclosure | test file path constants | accept | Paths are local dev paths only; test files never deployed to production |
</threat_model>

<verification>
- `composer test -- --filter DoctorPortal` exits with test failures (not PHP errors)
- All 8 methods listed in `--filter DoctorPortal` output
- No syntax error in DoctorPortalTest.php: `php -l tests/Unit/DoctorPortal/DoctorPortalTest.php`
</verification>

<success_criteria>
DoctorPortalTest.php exists with 8 @group DoctorPortal test methods that perform static file analysis. All 8 are RED (failing) before Wave 1 implementation begins. The file parses without PHP syntax errors.
</success_criteria>

<output>
After completion, create `.planning/phases/05-doctor-portal/05-00-SUMMARY.md`
</output>

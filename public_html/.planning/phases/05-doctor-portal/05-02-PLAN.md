---
phase: 05-doctor-portal
plan: "02"
type: execute
wave: 1
depends_on:
  - "05-01"
files_modified:
  - application/modules/doctor_chamber/views/doctor/dashboard.php
  - application/assets/css/chamber-practice.css
autonomous: true
requirements:
  - UI-09
  - UI-10

must_haves:
  truths:
    - "Dashboard view has exactly 4 stat cards in .chamber-stat-grid (total_today, checked_in, pending, today_revenue)"
    - "Stat card icons are in correct order: fa-calendar-day, fa-user-check, fa-hourglass-half, fa-coins"
    - "Queue panel is col-md-8 with empty #liveQueueBody tbody — no PHP foreach($queue_today) loop"
    - "Long-poll JS calls doctor_chamber/queue_json every 12000ms, updates #liveQueueBody and #queueLastUpdated"
    - "No AI strip (.chamber-ai-strip) present in view"
    - "No quick-action links row present in view"
    - "All header buttons use btn btn-sm classes"
  artifacts:
    - path: "application/modules/doctor_chamber/views/doctor/dashboard.php"
      provides: "Stat grid (UI-09) + queue panel (UI-10 partial) matching UI-SPEC component contracts"
      contains: "liveQueueBody"
    - path: "application/assets/css/chamber-practice.css"
      provides: "Spacing tweaks: stat-grid gap 12px, panel-header padding 12px 16px, panel margin-bottom 16px"
  key_links:
    - from: "dashboard.php JS setInterval"
      to: "Doctor_chamber::queue_json()"
      via: "$.getJSON(BASE_URL + 'doctor_chamber/queue_json')"
      pattern: "queue_json"
---

<objective>
Rewrite dashboard.php to match the UI-SPEC component contracts for the stat grid (UI-09) and the two-column queue + right-panel layout (UI-10 partial). Apply the three spacing tweaks to chamber-practice.css. The right panel HTML (follow-up count + high-risk list) and the chart canvases are written in this plan; their data wiring comes from Plans 03 and 04.

Purpose: Deliver the visible UI for UI-09 and the live queue panel. After this plan the dashboard renders accurate stat cards and populates the queue via long-poll.
Output: Fully rewritten dashboard.php matching UI-SPEC; minor CSS updates in chamber-practice.css.
</objective>

<execution_context>
@/c/Users/Samiul/Pictures/doctor/public_html/.planning/../../../get-shit-done/workflows/execute-plan.md
@/c/Users/Samiul/Pictures/doctor/public_html/.planning/../../../get-shit-done/templates/summary.md
</execution_context>

<context>
@/c/Users/Samiul/Pictures/doctor/public_html/.planning/PROJECT.md
@/c/Users/Samiul/Pictures/doctor/public_html/.planning/phases/05-doctor-portal/05-UI-SPEC.md
@/c/Users/Samiul/Pictures/doctor/public_html/.planning/phases/05-doctor-portal/05-RESEARCH.md

<interfaces>
<!-- Key $data variables available in dashboard.php after Plan 01 -->
<!-- From Doctor_chamber::dashboard() -->

```php
// All these are now set in $data after Plan 01:
$total_today       // (int)   — appointment COUNT for today, this doctor
$checked_in        // (int)   — queue COUNT status IN ('arrived','serving') today
$pending           // (int)   — queue COUNT status = 'pending' today
$today_revenue     // (float) — SUM doctor_amount from payment, Unix timestamp today
$followup_count    // (int)   — COUNT from patient_practice_tag join, tag='follow_up'
$high_risk_patients// array   — rows with patient_id, guest_name; tag='high_risk', limit 10
$settings          // object  — has $settings->currency
$doctor            // object  — doctor row
```

<!-- Stat grid order from UI-SPEC (locked) -->
<!-- Card 1: fa-calendar-day | $total_today | "Total patients today" -->
<!-- Card 2: fa-user-check   | $checked_in  | "Checked in"          -->
<!-- Card 3: fa-hourglass-half| $pending    | "Pending"             -->
<!-- Card 4: fa-coins        | $today_revenue (currency + number_format) | "Today's revenue" -->

<!-- Long-poll JS — exact contract from UI-SPEC Interaction Contracts -->
<!-- pollQueue() fires immediately, then every 12000ms -->
<!-- Updates tbody#liveQueueBody innerHTML and span#queueLastUpdated textContent -->
<!-- escHtml() helper must be inlined in the IIFE -->

<!-- Chart canvases — include in HTML this plan, wired in Plan 04 -->
<!-- <canvas id="chartMonthlyRevenue" height="220"> -->
<!-- <canvas id="chartMonthlyPatients" height="220"> -->
<!-- Chart.js CDN: https://cdn.jsdelivr.net/npm/chart.js@3.9.1/dist/chart.min.js -->
<!-- Add CDN <script> tag at bottom of file (before closing chart init block) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite dashboard.php to UI-SPEC layout (stat grid + two-column layout + chart canvases)</name>
  <files>application/modules/doctor_chamber/views/doctor/dashboard.php</files>
  <read_first>
    - application/modules/doctor_chamber/views/doctor/dashboard.php — read the FULL current view to understand existing structure before replacing
    - .planning/phases/05-doctor-portal/05-UI-SPEC.md sections "Page Header", "Top Stat Cards", "Main Layout: Two-Column", "Bottom: Charts Row", "Interaction Contracts: Live Queue Long-Poll", "Chart.js Bar Chart Initialization"
  </read_first>
  <action>
Replace the ENTIRE content of dashboard.php with the layout defined in 05-UI-SPEC.md. Do NOT preserve any old content. The new file must include all of these sections in order:

**SECTION 1 — Page header** (from UI-SPEC "Page Header"):
- `.chamber-ui` wrapper
- `section.content-header` > `.chamber-head` with kicker, h1, subtitle, and three action buttons
- Buttons use `btn btn-sm btn-primary` / `btn btn-sm btn-outline-primary` / `btn btn-sm btn-outline-secondary`
- Subtitle copy: "Today's queue, checked-in patients, and revenue at a glance."
- DO NOT include `.chamber-ai-strip` or the quick-action links row

**SECTION 2 — Stat grid** (from UI-SPEC "Top Stat Cards"):
- `section.content` opens here
- `.chamber-stat-grid` with exactly 4 `.chamber-stat` cards in the locked icon/variable/label order:
  1. `fa-calendar-day` / `$total_today` / "Total patients today"
  2. `fa-user-check` / `$checked_in` / "Checked in"
  3. `fa-hourglass-half` / `$pending` / "Pending"
  4. `fa-coins` / `<?php echo htmlspecialchars($settings->currency); ?> <?php echo number_format((float) $today_revenue, 0); ?>` / "Today's revenue"

**SECTION 3 — Two-column layout** (from UI-SPEC "Main Layout: Two-Column"):
- `div.row` containing:
  - `div.col-md-8` — `.chamber-panel` with `#liveQueueTable` and `<tbody id="liveQueueBody"></tbody>` (EMPTY — no PHP foreach)
  - `div.col-md-4` — two `.chamber-panel` cards:
    - Follow-up count panel: `$followup_count` in `.chamber-stat-value`, label "Patients with follow-up tag"
    - High-risk patients panel: PHP foreach over `$high_risk_patients` rendering name + `.chamber-status.high_risk` badge

**SECTION 4 — Chart row** (from UI-SPEC "Bottom: Charts Row"):
- `div.row` with two `div.col-md-6` panels:
  - `<canvas id="chartMonthlyRevenue" height="220"></canvas>`
  - `<canvas id="chartMonthlyPatients" height="220"></canvas>`

**SECTION 5 — Scripts**:
- Chart.js CDN: `<script src="https://cdn.jsdelivr.net/npm/chart.js@3.9.1/dist/chart.min.js"></script>`
- Long-poll IIFE (exact code from UI-SPEC "Interaction Contracts: Live Queue Long-Poll" — copy verbatim, wrapped in `<script>` tags)
- Chart init block (from UI-SPEC "Chart.js Bar Chart Initialization" — copy verbatim, wrapped in `<script>` tags)
- The `<?php echo BASE_URL; ?>` pattern — use `<?php echo base_url(); ?>` and assign to JS var `var BASE_URL = '<?php echo base_url(); ?>';` at top of script block

Use the EXACT HTML from the UI-SPEC component contracts. Do not invent alternative class names.
  </action>
  <verify>
    <automated>cd /c/Users/Samiul/Pictures/doctor/public_html && composer test -- --filter "test_ui09|test_ui10" 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "chamber-stat-value" application/modules/doctor_chamber/views/doctor/dashboard.php` returns 4 (exactly 4 stat value elements — right panel uses chamber-stat-value too for followup_count, so this should be >= 4)
    - `grep -c "liveQueueBody" application/modules/doctor_chamber/views/doctor/dashboard.php` returns 1
    - `grep -c "foreach.*queue_today" application/modules/doctor_chamber/views/doctor/dashboard.php` returns 0 (no server-render loop)
    - `grep -c "queue_json" application/modules/doctor_chamber/views/doctor/dashboard.php` returns >= 1
    - `grep -c "setInterval" application/modules/doctor_chamber/views/doctor/dashboard.php` returns >= 1
    - `grep -c "12000" application/modules/doctor_chamber/views/doctor/dashboard.php` returns 1
    - `grep -c "chamber-ai-strip" application/modules/doctor_chamber/views/doctor/dashboard.php` returns 0
    - `grep -c "chartMonthlyRevenue" application/modules/doctor_chamber/views/doctor/dashboard.php` returns >= 1
    - `grep -c "chart_data_json" application/modules/doctor_chamber/views/doctor/dashboard.php` returns >= 1
    - `grep -c "btn-sm" application/modules/doctor_chamber/views/doctor/dashboard.php` returns >= 3
    - test_ui09_stat_cards_count is GREEN
    - test_ui10_live_queue_body is GREEN
    - test_ui10_no_server_rendered_queue_loop is GREEN
  </acceptance_criteria>
  <done>dashboard.php is fully rewritten to UI-SPEC. Stat grid has 4 cards with correct icons. Two-column layout present. Queue tbody is empty (JS-populated). Chart canvases present. Long-poll JS included. No AI strip. No quick-action links row.</done>
</task>

<task type="auto">
  <name>Task 2: Apply spacing tweaks to chamber-practice.css</name>
  <files>application/assets/css/chamber-practice.css</files>
  <read_first>
    - application/assets/css/chamber-practice.css — find lines with .chamber-stat-grid gap, .chamber-panel-header padding, and .chamber-panel margin-bottom to locate exact values before editing
  </read_first>
  <action>
Apply exactly three CSS value changes (per UI-SPEC Spacing Scale section):

1. `.chamber-stat-grid` — change `gap: 14px` to `gap: 12px`
2. `.chamber-panel-header` — change padding value from `14px` to `12px 16px` (vertical → horizontal split)
3. `.chamber-panel` — change `margin-bottom: 18px` to `margin-bottom: 16px`

These are targeted value edits only. Do NOT restructure or reformat the CSS file. If any of these values are already at the target value, leave them as-is and note in commit message.
  </action>
  <verify>
    <automated>cd /c/Users/Samiul/Pictures/doctor/public_html && php -l application/modules/doctor_chamber/views/doctor/dashboard.php &amp;&amp; echo "PHP syntax OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gap: 12px" application/assets/css/chamber-practice.css` returns >= 1 (stat-grid gap updated)
    - `grep -c "gap: 14px" application/assets/css/chamber-practice.css` returns 0 (old value removed)
    - `grep -c "margin-bottom: 16px" application/assets/css/chamber-practice.css` returns >= 1
    - `grep -c "margin-bottom: 18px" application/assets/css/chamber-practice.css` returns 0 (old value removed)
  </acceptance_criteria>
  <done>chamber-practice.css has three spacing values updated to 4px-grid-aligned values per UI-SPEC Spacing Scale section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| queue_json response → innerHTML | JS receives JSON from authenticated endpoint; escHtml() applied to all string fields before DOM injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering / XSS | queue table innerHTML injection | mitigate | escHtml() IIFE helper applied to guest_name, chamber_name, status, triage_summary before concat into HTML string |
| T-05-02-02 | Information Disclosure | patient_id in Open button href | accept | patient_id is an integer; doctor already has access to these patients via authenticated dashboard; no PII exposed in URL beyond integer ID |
</threat_model>

<verification>
- `composer test -- --filter DoctorPortal` — test_ui09_stat_cards_count, test_ui10_live_queue_body, test_ui10_no_server_rendered_queue_loop are GREEN
- Manual smoke: Load doctor dashboard in browser — 4 stat cards visible, queue table present, no PHP errors
- `grep -v "^//" application/assets/css/chamber-practice.css | grep -c "gap: 14px"` returns 0 (old gap gone)
</verification>

<success_criteria>
dashboard.php fully matches UI-SPEC component contracts for stat grid and queue panel. Chamber-practice.css has updated spacing. Five PHPUnit tests are GREEN (test_ui09 x2, test_ui10 x3).
</success_criteria>

<output>
After completion, create `.planning/phases/05-doctor-portal/05-02-SUMMARY.md`
</output>

---
phase: 01-security-hardening
plan: "02"
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - application/modules/ai_image_analysis/controllers/Ai_image_analysis.php
  - application/modules/ambulance/controllers/Ambulance.php
autonomous: true
requirements: [SEC-04, SEC-07]

must_haves:
  truths:
    - "No HTTP request can reach debugSession() — the method does not exist in the class"
    - "No browser request to Ai_image_analysis or Ambulance renders raw PHP structure dumps or session data"
    - "The unnamed debug block (lines ~487–548) that echoes Database connected and session data is deleted"
    - "debugRates() method is deleted from Ambulance.php"
  artifacts:
    - path: "application/modules/ai_image_analysis/controllers/Ai_image_analysis.php"
      provides: "Controller with debug methods removed"
    - path: "application/modules/ambulance/controllers/Ambulance.php"
      provides: "Controller with debugRates() removed"
  key_links:
    - from: "attacker HTTP request"
      to: "Ai_image_analysis::debugSession()"
      via: "CI3 routing"
      pattern: "function debugSession"
---

<objective>
Remove all unauthenticated debug endpoints and echo/print_r debug output from Ai_image_analysis.php and Ambulance.php (SEC-04 + partial SEC-07).

Purpose: `debugSession()` outputs full session data, hospital_id, and DB record counts to any unauthenticated caller. The unnamed debug block above it does the same. `debugRates()` in Ambulance.php dumps DB table structure and session data. All are information disclosure endpoints with no production purpose.

Output: Both files with all debug methods deleted and no `echo print_r` or `echo "Database error"` statements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-hardening/01-RESEARCH.md

<interfaces>
<!-- Exact line ranges to delete — extracted from codebase -->

Ai_image_analysis.php — three debug blocks to delete entirely:

Block 1 (unnamed debug method, lines ~487–549):
Starts with the line before `echo "User ID: "` (the method opening brace following the function signature)
and ends at the closing `}` of the `catch` block at line ~548.
This block contains: `echo "Database connected"`, `echo "Session data: " . print_r(...)`, `echo "Hospital ID:"`, `echo "Table exists"`, `echo "Total records:"`, `echo "Records for hospital:"`, `echo "Sample records:"`.
The method also contains `testAnalyticsNoAuth()` (lines ~551–593) and `getAnalyticsHistorySimple()` (lines ~595–613) — both have ENVIRONMENT guards but still contain echo statements; delete these methods entirely.

Block 2 — debugSession() method (lines 615–642):
```php
public function debugSession()
{
    if (defined('ENVIRONMENT') && ENVIRONMENT === 'production') {
        show_404();
    }
    // Debug session data
    echo "Session data:<br>";
    ...
    echo "Total records in ai_image_analyses: " . $total . "<br>";
    ...
}
```
Delete the entire method from `public function debugSession()` through its closing `}`.

Ambulance.php — one debug method to delete (lines 739–767):
```php
// Debug method to check database table
public function debugRates()
{
    ...
    echo "<pre>" . print_r($tables, true) . "</pre>";
    ...
    echo "<pre>" . print_r($fields, true) . "</pre>";
    ...
    echo "<pre>" . print_r($rates, true) . "</pre>";
    ...
    echo "<pre>" . print_r($this->session->userdata(), true) . "</pre>";
}
```
Delete from the comment line `// Debug method to check database table` through the closing `}` of `debugRates()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete debugSession(), testAnalyticsNoAuth(), getAnalyticsHistorySimple(), and unnamed debug block from Ai_image_analysis.php — SEC-04 + SEC-07</name>
  <files>application/modules/ai_image_analysis/controllers/Ai_image_analysis.php</files>

  <read_first>
    - application/modules/ai_image_analysis/controllers/Ai_image_analysis.php lines 480–650 (contains all debug methods to delete: unnamed block ~487–548, testAnalyticsNoAuth ~551–593, getAnalyticsHistorySimple ~595–613, debugSession 615–642)
    - application/modules/ai_image_analysis/controllers/Ai_image_analysis.php lines 644–680 (getAnalyticsHistoryDataTables — this is the FIRST legitimate method after the debug section; deletion must stop before it)
  </read_first>

  <action>
Read the file and identify the exact start and end lines of each debug method. Delete:

1. **Unnamed debug block** — the method that starts around line 487 with something like `public function testAnalytics()` or a similar name containing the `echo "Database connected"` block. Read lines 480–490 to find the exact function name and opening. Delete from that function signature line through the closing `}` at approximately line 548.

2. **`testAnalyticsNoAuth()`** — starts around line 551 with `public function testAnalyticsNoAuth()`. Delete from this line through its closing `}` at approximately line 593.

3. **`getAnalyticsHistorySimple()`** — starts around line 595 with `public function getAnalyticsHistorySimple()`. Delete from this line through its closing `}` at approximately line 613.

4. **`debugSession()`** — starts at line 615 with `public function debugSession()`. Delete from this line through its closing `}` at line 642.

After deletion, the method immediately following this block should be `public function getAnalyticsHistoryDataTables()` (around line 644). Verify this method is intact.

Run `php -l` after edit.

**Do not delete any method that does not contain `echo print_r`, `echo "Database`, `echo "Session data`, `echo "Total records`, or `echo "Hospital ID` output statements.**
  </action>

  <verify>
    <automated>cd "C:/Users/Samiul/Pictures/doctor/public_html" && php -l application/modules/ai_image_analysis/controllers/Ai_image_analysis.php && grep -c "function debugSession" application/modules/ai_image_analysis/controllers/Ai_image_analysis.php && grep -c "echo print_r" application/modules/ai_image_analysis/controllers/Ai_image_analysis.php && grep -c "echo \"Database connected" application/modules/ai_image_analysis/controllers/Ai_image_analysis.php</automated>
  </verify>

  <acceptance_criteria>
    - `php -l application/modules/ai_image_analysis/controllers/Ai_image_analysis.php` outputs "No syntax errors detected"
    - `grep -c "function debugSession" application/modules/ai_image_analysis/controllers/Ai_image_analysis.php` returns 0
    - `grep -c "echo print_r" application/modules/ai_image_analysis/controllers/Ai_image_analysis.php` returns 0
    - `grep -c "echo \"Database connected" application/modules/ai_image_analysis/controllers/Ai_image_analysis.php` returns 0
    - `grep -c "function getAnalyticsHistoryDataTables" application/modules/ai_image_analysis/controllers/Ai_image_analysis.php` returns 1 (legitimate method preserved)
  </acceptance_criteria>

  <done>All four debug methods deleted from Ai_image_analysis.php. No echo/print_r debug output remains. getAnalyticsHistoryDataTables() and all other production methods are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Delete debugRates() from Ambulance.php — SEC-07</name>
  <files>application/modules/ambulance/controllers/Ambulance.php</files>

  <read_first>
    - application/modules/ambulance/controllers/Ambulance.php lines 735–790 (debugRates() method at lines 739–767, followed by legitimate getAvailableAmbulances() at ~769)
  </read_first>

  <action>
In `application/modules/ambulance/controllers/Ambulance.php`:

Delete the entire `debugRates()` method including the comment line before it:
```
// Debug method to check database table
public function debugRates()
{
    ...  (all lines through the closing })
}
```

This block runs from approximately line 739 (`// Debug method to check database table`) through line 767 (closing `}` of `debugRates()`).

The method immediately following (`// AJAX Functions` comment + `public function getAvailableAmbulances()`) must be preserved intact.

Run `php -l` after edit.
  </action>

  <verify>
    <automated>cd "C:/Users/Samiul/Pictures/doctor/public_html" && php -l application/modules/ambulance/controllers/Ambulance.php && grep -c "function debugRates" application/modules/ambulance/controllers/Ambulance.php && grep -c "echo print_r" application/modules/ambulance/controllers/Ambulance.php && composer test -- --filter "test_sec04|test_sec07" 2>&1 | grep -E "test_sec04|test_sec07|OK|FAIL|pass"</automated>
  </verify>

  <acceptance_criteria>
    - `php -l application/modules/ambulance/controllers/Ambulance.php` outputs "No syntax errors detected"
    - `grep -c "function debugRates" application/modules/ambulance/controllers/Ambulance.php` returns 0
    - `grep -c "echo print_r" application/modules/ambulance/controllers/Ambulance.php` returns 0
    - `grep -c "function getAvailableAmbulances" application/modules/ambulance/controllers/Ambulance.php` returns 1 (preserved)
    - `composer test -- --filter "test_sec04|test_sec07"` — test_sec04 passes; test_sec07 passes (Inventory still has debug output — test only checks Ai_image_analysis and Ambulance at this point; full pass after plan 03 completes)
  </acceptance_criteria>

  <done>debugRates() deleted from Ambulance.php. No echo/print_r debug output remains in either Ai_image_analysis.php or Ambulance.php.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unauthenticated HTTP → Ai_image_analysis::debugSession() | Any caller could access full session dump |
| Authenticated HTTP → Ambulance::debugRates() | Any logged-in user could access DB schema + session data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | Ai_image_analysis::debugSession() | mitigate | Delete method entirely per SEC-04 |
| T-01-02-02 | Information Disclosure | Ai_image_analysis (unnamed debug block) | mitigate | Delete entire method body per SEC-07 |
| T-01-02-03 | Information Disclosure | Ambulance::debugRates() | mitigate | Delete method entirely per SEC-07 |
</threat_model>

<verification>
- `grep -rn "function debugSession" application/modules/ai_image_analysis/` returns 0 results
- `grep -rn "echo print_r" application/modules/ai_image_analysis/ application/modules/ambulance/` returns 0 results
- Both files pass `php -l`
- `composer test -- --filter SecurityPatch` — test_sec04 and test_sec07 pass for Ai_image_analysis and Ambulance
</verification>

<success_criteria>
debugSession() is gone. The unnamed debug block is gone. debugRates() is gone. No unauthenticated session/DB dump endpoint exists in these two modules.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-hardening/01-02-SUMMARY.md`
</output>
